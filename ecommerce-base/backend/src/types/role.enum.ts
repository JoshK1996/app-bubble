/**
 * Role enum defines possible user roles in the E-commerce application.
 */
export enum Role {
  CUSTOMER = 'CUSTOMER', // Role for standard users browsing/buying products
  ADMIN = 'ADMIN',     // Role for administrators managing the platform
  // Add other roles specific to e-commerce if needed (e.g., VENDOR, SUPPORT)
} 