module.exports = {
  printWidth: 100, // Fit code within 100 characters
  tabWidth: 2, // Use 2 spaces for indentation
  useTabs: false, // Use spaces instead of tabs
  semi: true, // Add semicolons at the end of statements
  singleQuote: true, // Use single quotes for strings
  trailingComma: 'all', // Add trailing commas wherever possible (es5, all, none)
  bracketSpacing: true, // Add spaces inside object literals { foo: bar }
  arrowParens: 'always', // Always include parentheses around arrow function parameters (e.g., (x) => x)
  endOfLine: 'auto', // Maintain existing line endings (auto | lf | crlf | cr)
}; 