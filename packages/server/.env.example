# Server Configuration
PORT=3001
NODE_ENV=development

# AWS Configuration
AWS_REGION=us-east-1
# If using AWS credentials directly (not recommended for production)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# RDS Database Connection
DB_HOST=your-rds-instance.amazonaws.com
DB_PORT=5432
DB_NAME=construction_tracker
DB_USER=postgres
DB_PASSWORD=your_password

# S3 Configuration
S3_BUCKET_NAME=construction-tracker-files

# Secret for JWT (if used for additional auth)
JWT_SECRET=your_jwt_secret 